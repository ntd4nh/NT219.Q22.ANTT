# Lab tuning: giu rule CRS mac dinh, bat engine On qua bien moi truong MODSEC_RULE_ENGINE.

# Webhook chi qua mTLS ingress (:8443)
SecRule REQUEST_URI "@streq /api/billing/webhook" "id:900010,phase:1,chain,deny,status:403,log,auditlog,msg:Webhook must use mTLS ingress on port 8443"
SecRule REQUEST_METHOD "@streq POST" "t:none,chain"
SecRule REQUEST_URI "@streq /api/billing/webhook" "t:none"
